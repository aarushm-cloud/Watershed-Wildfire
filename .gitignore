# Inputs and generated artifacts
/data/
/out/

# Large reconstruction rasters (Step 1); vectors (*.geojson) stay tracked
/validation/data/*.tif
/validation/data/*.tiff
/validation/data/.osm_cache/

# Gate-regenerated outputs -- the behavior lock is the oracle, not these files (OUT-CLEANUP)
/validation/out/

# osmnx request cache (P3 asset acquisition) -- never staged
/cache/

# Python
__pycache__/
*.py[cod]

# OS / editor
.DS_Store

/tests/fixtures

# Extra
CLAUDE.md
DECISIONS.md
/.claude/commands